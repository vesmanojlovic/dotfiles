unbind C-b
set -g prefix C-p
bind C-l send-keys 'C-l' # clear screen

unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"

set -g mouse on

# --- VIM NAVIGATOR ---
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
unbind -T copy-mode-vi Space
unbind -T copy-mode-vi Enter
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel "wl-copy"

# --- WINDOW/PANE MANAGEMENT ---
set -g base-index 1
set-window-option -g pane-base-index 1
set-window-option -g mode-keys vi

unbind %
unbind '"'
bind v split-window -h -c "#{pane_current_path}"
bind h split-window -v -c "#{pane_current_path}"

set -g history-limit 100000

unbind n
unbind w
bind w new-window -c "#{pane_current_path}"
bind n command-prompt "rename-window '%%'"
bind-key j previous-window
bind-key k next-window

set -g status-right-length 150

# --- PLUGINS ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @resurrect-strategy-nvim 'session'

# --- COLORS & RENDERING ---
# 1. Enable True Color (Crucial for Pywal/Neovim)
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# 2. Load the Dynamic Pywal Theme
# This file handles ALL status bar formatting and colors now.
source-file ~/.cache/wal/colors-tmux.conf

# Initialize TMUX plugin manager (keep this at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
